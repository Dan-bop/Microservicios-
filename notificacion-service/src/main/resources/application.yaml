server:
  port: 8084

spring:
  application:
    name: notificacion-service

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USER}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

empresa:
  nombre: Cable Ebenecer Trujillo
  email: ${MAIL_USER}
  datos-pago: "Yape/Plin: 987 298 465 | Oficina: Tu Corazon Bebe"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost
